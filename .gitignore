*.o
*.obj
*.a
*.so
*.out
*.d
obj/
ircserv

.DS_Store
*.swp
*~
.vscode/
.idea/

# clangd / editor (local only, keep the dev push to source files)
compile_flags.txt
.cache/

compile_commands.json

# ローカル専用の設計ドキュメント（push しない）
docs/

# 検証ハーネス：ソースは追跡（CIで実行）、ビルド成果物のみ除外
verify/verify
verify/verify_cov
verify/*.profraw
verify/*.profdata
verify/*.dSYM/
